# Processes

Real `.bwp` BusinessWorks process files go here (e.g. `ProcessOrder.bwp`),
authored in TIBCO Business Studio. Empty placeholder for now — the stand-in
build logic that exercises the pipeline lives in
`../../../../src/main/java/com/acme/orders/module/`.
