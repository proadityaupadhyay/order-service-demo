# Runtime properties for dev, injected at deploy time via `docker run --env-file`
# (see deploy/scripts/deploy.sh). Distinct from the build-time
# app/OrderService.application/configuration/dev.substvar, which is baked
# into the image at build time.
PORT=8080
APP_ENV=dev
LOG_LEVEL=DEBUG
DOWNSTREAM_INVENTORY_URL=http://mock-service:9090
